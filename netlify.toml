[build]
  base = "grazelink"
  command = "npm run build"
  publish = "dist"

# SPA fallback: serve index.html for every path so React Router
# handles client-side routes on refresh/deep links.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
